import Registration from "@/pages/registration";

// This page is for the enrollment.getmydpc.com subdomain
// It redirects to the registration flow
export default function Enrollment() {
  return <Registration />;
}