
[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm --prefix client install && npm --prefix client run build && npm run build"]

[start]
cmd = "npm start"
